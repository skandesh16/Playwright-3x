// Ternary Operator (Condition)
// Condition means Either this or Either that
// Condition ? Value(if true) : value(if false)

let age = 26;
let Is_Skandesh_old_enough_to_Marraige = age >= 27 ? true : false;
console.log(Is_Skandesh_old_enough_to_Marraige);

