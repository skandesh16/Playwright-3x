var name = 'Skandesh Pachipulusu';

var f_name = 'Skandesh';
var l_Name = 'Pachipulusu'; // Cammel case

// Naming Conversations
//=========================================
// 1. CammelCase (Standrad for JS variable and Function)
let userName = 'camelCase';
let userPassword = '***';
let totalPrice = $999;

// 2. PascalCase (Standard for JS Classes and constructors)
let UserProfile = 'AutomationTester';
let LoginPage = 'LoginPage';
let FinalCartPrice = '1999';

// 3. Snake_case (Underscore in between)
let User_name = 'playwrightTester';
let fulel_type = 'Diesel';
let Fuel_Amount = $75;

// 4. SCREMEING_SNAKE_CASE (used only with constants)
const RUPEE_VALUE = 'inr';
const WEBSITE_URL = 'https://www.demoblaze.com';

// 5. Hungarion Notation (prefix with type -older style not much used now)
let strUserName = 'String';
let bActive = 'true' //Boolean 
let nCount = 5 //number
let arrItems = [] //array

let strFristName = 'Pachhipulasu';

// Sardhar ji = sing 
